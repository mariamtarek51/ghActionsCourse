name: first workflow

on:
  push:
    branches:
      - main
jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print a welcome message
        run : echo "welcome on out first github actions wworkflow"
        
      - name: checkout Repoistory
        uses: actions/checkout@v6.0.2

      - name: list repo files
        run : ls -la
      
  
