function greet(name) {

return `Hello, ${name}!`;
}
module. exports = greet;
// Ensure function is exportable
// If run directly, print Output (useful for debugging)
//if (require.main ===module) {
//console. tog (greet( "World" ) ) ;
//}
